import AIRevenueOSLandingPage from "./AiRevenueOsPageClient";

/** Server-friendly entry: delegates to the client landing (providers, dynamic chunks). */
export function AIRevenueOsPageGate() {
  return <AIRevenueOSLandingPage />;
}
