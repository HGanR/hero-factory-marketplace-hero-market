"use client";

/** Placeholder until handoff export APIs are wired; UI shell only. */
export function TaxPreparerHandoffPanel() {
  return (
    <div className="rounded-lg border border-slate-800 bg-slate-950/40 p-4 text-sm text-slate-400">
      Tax preparer handoff is not configured for this workspace yet.
    </div>
  );
}
