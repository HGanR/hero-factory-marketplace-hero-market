"use client";

/** Placeholder until review queue APIs are wired; UI shell only. */
export function AccountingReviewQueuePanel() {
  return (
    <div className="rounded-lg border border-slate-800 bg-slate-950/40 p-4 text-sm text-slate-400">
      Review queue is not configured for this workspace yet.
    </div>
  );
}
