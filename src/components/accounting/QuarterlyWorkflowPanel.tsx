"use client";

/** Placeholder until quarterly workflow APIs are wired; UI shell only. */
export function QuarterlyWorkflowPanel() {
  return (
    <div className="rounded-lg border border-slate-800 bg-slate-950/40 p-4 text-sm text-slate-400">
      Quarterly workflow is not configured for this workspace yet.
    </div>
  );
}
