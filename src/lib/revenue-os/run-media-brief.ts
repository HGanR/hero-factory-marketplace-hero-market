/**
 * Compile media brief from campaign + notes — same POST as `CampaignFromNotesSection` “Compile Media Brief”.
 * @see BENTLEY_ACTION_MAP
 */
export { runCompileMediaBriefApi as runCompileMediaBrief } from "./revenue-os-pipeline-actions";
