/**
 * Generate structured campaign from pasted notes — same POST as `CampaignFromNotesSection` “Generate”.
 * @see BENTLEY_ACTION_MAP
 */
export { runCampaignFromNotesApi as runCampaignFromNotes } from "./revenue-os-pipeline-actions";
